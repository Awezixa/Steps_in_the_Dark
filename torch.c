#include <stdio.h>
#include "torch.h"

void TorchInteract() {
        printf("The torch is already lit.\n");
    } else {
        printf("You light the torch. It illuminates your surroundings.\n");
    }
    else if
}

